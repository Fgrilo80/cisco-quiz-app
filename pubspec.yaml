name: cisco_quiz
description: Cisco Quiz — prática CCST, CCNA e CCNP em Português e English.
publish_to: "none"
version: 1.2.1+3

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/cricket.json
